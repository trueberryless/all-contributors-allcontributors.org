---
title: Contributing
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

This guide explains our contribution guidelines and how you can contribute to the All Contributor projects. There are many ways to contribute, from ideas over coding to

:::tip[Call for translators!]
[We're looking for translators](https://github.com/all-contributors/allcontributors.org/issues/143) to help translate this spec for everyone!
:::

## Help improve the All Contributors documentation!

import GitHubBadges from "@/components/GitHubBadges.astro";

<GitHubBadges repo="all-contributors/allcontributors.org" />

Thanks for your interest in the project!
Pull Requests are welcome for any type of improvement, from a small typo to a new section in the docs. Help us make the project better!

All of the source code for the documentation is available in [this repository](https://github.com/all-contributors/allcontributors.org).
The All Contributors bot lives in the [app repository](https://github.com/all-contributors/app).
And the All Contributors CLI lives in the [cli repository](https://github.com/all-contributors/cli).

## How to commit

We use the [angular commit convention](https://github.com/angular/angular/blob/main/CONTRIBUTING.md#-commit-message-guidelines) for commits that are used for an automated changelog generation, so it would be advisable to respect that.
If you're not used to it or are afraid to mistype a commit, you can run `npm run commit` or `git cz` (if you have installed `commitizen`) which would take you through a few questions and write the commit for you.

## Editing Content

:::tip
Every page has an _Edit_ button up the bottom, clicking this will take you straight to the GitHub source code for that page.
:::

Once on a file, click the 'pencil' icon to [easily edit the file inline](https://help.github.com/articles/editing-files-in-your-repository/).
After making your changes, create a commit with your changes. You will want to create and commit these changes on a **new branch** in your fork of the repository.

After committing your changes, you can create a pull request to propose your changes to be merged into the main branch.

![A screenshot that shows the GitHub interface with a file open focused on the pencil icon that allows you to edit the page.](@/assets/project/edit-this-page.png)

## Translations

[![Crowdin](https://d322cqt584bo4o.cloudfront.net/all-contributors/localized.svg)](https://crowdin.com/project/all-contributors)

Our translations are managed through crowdin. You can help contribute by heading to [the crowdin project] and suggesting translations.
If you like, [comment on this issue](https://github.com/all-contributors/allcontributors.org/issues/143) to let us know you're helping or if you have any queries!

> When translating, anything that has the [XPATH](https://developer.mozilla.org/en-US/docs/Web/XPath) `@href`, `@src` etc mustn't be translated. Things like `code` should not be either. This will cause the project to 404. See the below image for an example:

![A screenshot that shows the GitHub interface with a file open focused on the pencil icon that allows you to edit the page.](@/assets/project/translating-xpath.png)

> However, it's recommended to translate any content that would contribute to a better comprehension like the comments in `code` tags _as long as_ it doesn't change the actual code since it only understands English.

### How to's

#### Login to Crowdin

1. Go to [the crowdin project], then [sign up](https://crowdin.com/join) and/or [log in](https://crowdin.com/login).
2. Join the "All Contributors" project and voila!

#### Translate strings

1. Go to [the crowdin project].
2. Click on the flag of the language you want to translate the documentation to.
3. Click on any `*.md` files that aren't completely translated (read showing 100% on the right-hand side)
4. Write your translation in the _"Enter translation here"_ field (which would correspond to the translation of the text in the _"SOURCE STRING"_ field above) then click "SAVE"
5. For **proofreaders** only: You can either approve translation suggestions (whether it's done by you or someone else), which you will find in the _"[LANGUAGE] TRANSLATIONS"_ section, by clicking on the :ballot_box_with_check: or delete them by clicking on the :wastebasket: icon.

### Change deployments

After you translate strings in any given language, you won't see those changes appearing live until the `main` branch was updated followed by a successful deployment.

## Run the website locally

All Contributors currently uses [Astro](https://docs.astro.build/) with [Starlight](https://starlight.astro.build/) for our documentation and website.

To build the docs locally:

<Steps>

1. Make sure `npm` is installed on your machine. Use the [nodejs documentation](https://docs.npmjs.com/downloading-and-installing-node-js-and-npm) if you aren't sure how to install `npm`.

    To check that npm is installed run the following in your favorite shell:

    ```sh
    npm -v
    ```

2. Next, make sure you have at least node version 0.22 (LTS) or higher. To check the version, use:

    ```sh
    node -v
    ```

    If you don't have atleast version `.22` or higher, please install node or upgrade your current version.

3. Install all dependencies:

    <Tabs syncKey="pkg">
    <TabItem label="npm">

    ```sh
    npm i
    ```

    </TabItem>
    <TabItem label="pnpm">

    ```sh
    pnpm i
    ```

    </TabItem>
    <TabItem label="Yarn">

    ```sh
    yarn i
    ```

    </TabItem>
    </Tabs>

4. Once you have completed the above, you can launch a server locally that will build and run the docs locally. In your favorite shell run:

    <Tabs syncKey="pkg">
    <TabItem label="npm">

    ```sh
    npm run start
    ```

    </TabItem>
    <TabItem label="pnpm">

    ```sh
    pnpm start
    ```

    </TabItem>
    <TabItem label="Yarn">

    ```sh
    yarn start
    ```

    </TabItem>
    </Tabs>

5. Then, go to `http://localhost:4321` and you will see the all contributors website there!

</Steps>

Refresh the browser page as needed when editing markdown pages.

### Linting and formatting your contribution

We use [Biome](https://biomejs.dev/) for linting and formatting files in the documentation project.
Currently, Biome does not support formatting Markdown or MDX files. You can format or lint other file types though.

There are several options to use Biome: You can lint, format, or lint and format, all three options either with automatic fixes or without.
Use the following commands for your desired goal:

<Tabs syncKey="pkg">
<TabItem label="npm">

- **Lint**: Scans for code smells and best-practice violations.

    ```sh
    npm run lint
    ```

- **Lint & Fix**: Scans and automatically applies safe fixes to your code.

    ```sh
    npm run lint:fix
    ```

- **Format**: Checks if your code matches the defined style (indentation, etc.).

    ```sh
    npm run format
    ```

- **Format & Fix**: Automatically reformats all files to match the style guide.

    ```sh
    npm run format:fix
    ```

- **Check**: Runs linting, formatting, and import organization (read-only).

    ```sh
    npm run check
    ```

- **Check & Fix**: Runs linting, formatting, and import organization and writes changes.

    ```sh
    npm run check:fix
    ```

</TabItem>
<TabItem label="pnpm">

- **Lint**: Scans for code smells and best-practice violations.

    ```sh
    pnpm lint
    ```

- **Lint & Fix**: Scans and automatically applies safe fixes to your code.

    ```sh
    pnpm lint:fix
    ```

- **Format**: Checks if your code matches the defined style (indentation, etc.).

    ```sh
    pnpm format
    ```

- **Format & Fix**: Automatically reformats all files to match the style guide.

    ```sh
    pnpm format:fix
    ```

- **Check**: Runs linting, formatting, and import organization (read-only).

    ```sh
    pnpm check
    ```

- **Check & Fix**: Runs linting, formatting, and import organization and writes changes.

    ```sh
    pnpm check:fix
    ```

</TabItem>
<TabItem label="Yarn">


- **Lint**: Scans for code smells and best-practice violations.

    ```sh
    yarn lint
    ```

- **Lint & Fix**: Scans and automatically applies safe fixes to your code.

    ```sh
    yarn lint:fix
    ```

- **Format**: Checks if your code matches the defined style (indentation, etc.).

    ```sh
    yarn format
    ```

- **Format & Fix**: Automatically reformats all files to match the style guide.

    ```sh
    yarn format:fix
    ```

- **Check**: Runs linting, formatting, and import organization (read-only).

    ```sh
    yarn check
    ```

- **Check & Fix**: Runs linting, formatting, and import organization and writes changes.

    ```sh
    yarn check:fix
    ```

</TabItem>
</Tabs>

### Checking links

We use the [Starlight Links Validator plugin](https://starlight-links-validator.vercel.app/) to check all our links within the documentation. This ensures that we don't include any links that lead to broken pages.

If you want to run the linkcheck locally, run the following command:

<Tabs syncKey="pkg">
<TabItem label="npm">

```sh
npm run linkcheck
```

</TabItem>
<TabItem label="pnpm">

```sh
pnpm linkcheck
```

</TabItem>
<TabItem label="Yarn">

```sh
yarn linkcheck
```

</TabItem>
</Tabs>

If your changes introduced any broken links, the build will fail and tell you which links you need to fix.

#### What is a broken link?

Here are some rule of thumbs for what is considered a broken link and how to fix them.

| Issue Type | What it means | How to fix it |
| :--- | :--- | :--- |
| **Relative Links** | Links starting with `./` or `../`. These are difficult to maintain when files move. | Convert to absolute paths (e.g., use `/en/project/contribute` instead of `./contribute`). |
| **Local Links** | Links pointing to `localhost` or `127.0.0.1`. | Remove development URLs and replace them with internal absolute paths. |
| **Missing Pages** | A link to an internal path that does not exist at all. | Fix the typo in the file path or create the missing documentation page. |
| **Invalid Hashes** | A link pointing to a `#section` that does not exist on the target page. | Verify the heading ID in the target file and update the anchor to match. |
| **Inconsistent Locale** | A link pointing to a different language (e.g., a Spanish page linking to an English one). | Update the link to use the correct language prefix (e.g., change `/en/setup` to `/es/setup`). |

## Add yourself as a contributor

To add yourself to the table of contributors, follow the [bot usage instructions](/en/bot/usage).

[the crowdin project]: https://crowdin.com/project/all-contributors
